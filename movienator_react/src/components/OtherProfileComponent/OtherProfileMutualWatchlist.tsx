export default function OtherProfileMutualWatchlist(props: any) {
  return (
    <div>
      List of movies both users have in their watchlist (can just use MoviesList
      with a title)
    </div>
  );
}
