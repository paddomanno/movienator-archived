export default function MoviesListOneLine(props: any) {
  return (
    <div>
      A List, like MoviesList, but only one-dimensional with arrow button.
      Should be used in other components that want to make a one-dimensional
      list.
    </div>
  );
}
