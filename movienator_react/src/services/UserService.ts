import { User } from '../types/User';

export async function getAllUsers(): Promise<User[]> {
  return [];
}

export async function getOneUser(userId: number): Promise<User | null> {
  return null;
}
